Act as a Senior Vue.js Developer. I recently did a mass find-and-replace in my codebase, changing a custom `lan()` function to Laravel's standard `__()` function.

I have set up `__()` as a global property in my Vue app (`vueApp.config.globalProperties.__`), which means it works perfectly inside `<template>` tags without any imports.

However, I need you to review my `.vue` and `.js` files to fix the usage of `__()` inside the `<script setup>` or standard `<script>` sections.

**Your tasks:**
1. **Templates:** Leave `__('String')` inside `<template>` tags exactly as they are. They are working fine.
2. **Scripts (`<script setup>`):** Since globalProperties are not directly accessible inside `<script setup>`, any usage of `__('String')` here will cause a "ReferenceError: __ is not defined".
   - Please replace `__('String')` inside `<script setup>` with an import from a composable.
   - Example:
     ```javascript
     import { useTranslation } from '@/composables/useTranslation';
     const { __ } = useTranslation();
     // Now __() can be used safely in the script.
     ```
3. **Variables interpolation:** Ensure that strings with variables (like `__('HIGH TAX: {amount}G', { amount: 50 })`) are correctly formatted as the second argument object, rather than using JS template literals inside the translation key.

Please provide the corrected files, ensuring the script logic is not broken. Here are my files:
resources/js/
resources/views/
